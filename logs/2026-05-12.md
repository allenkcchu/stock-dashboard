# 工作日誌 2026-05-12

## 完成項目

### 修正 FORCE_SEND 表達式（昨日修法無效的根本原因）

**症狀**：今早推播再次未發送，且今天完全無 run 紀錄（或 run 嚴重延遲）。

**根本原因**：昨日 2026-05-11 記錄的修法，其 GitHub Actions 表達式：

```yaml
FORCE_SEND: ${{ github.event_name == 'schedule' && 'true' || github.event.inputs.force }}
```

在 schedule 觸發時，`FORCE_SEND` 實際評估為空字串（非 `'true'`），導致
`is_market_open_window()` 仍然生效，加上 GitHub Actions cron 延遲 2-3 小時，
執行時已是 13:09 ET，不在 9:20–9:59 窗口內，直接 skip。

昨天兩個 scheduled run（UTC 16:13、17:08）都顯示 `success`，
但 log 內均為 `Not in market open window, skipping`，實際未推播。

**修正**：改用 fallback 寫法，schedule 觸發時 `inputs.force` 為空，自動 fallback 到 `'true'`：

```yaml
FORCE_SEND: ${{ github.event.inputs.force || 'true' }}
```

---

### 台灣大盤功能開發（開始）

- 確定加入指標：加權指數 TWII、費半 SOX、VIX、台積電 2330.TW、三大法人買賣超、外資期貨淨部位、PCR
- 架構決定：yfinance 取價格（5y），三大法人 / 期貨 / PCR 存 CSV 於 repo，GitHub Actions 每日更新
- 建立 `data/taiwan.py`：TWSE BFI82U + TAIFEX CSV DateDown endpoint
- 建立 `scripts/init_taiwan_data.py`、`update_taiwan_daily.py`、`patch_taifex_data.py`
- 建立 `.github/workflows/update_taiwan.yml`（UTC 10:00 = 台灣 18:00 更新）
- 執行 `init_taiwan_data.py`，取得 1306 列資料（2021-05 ~ 2026-05）

## 遇到的問題與解法

| 問題 | 解法 |
|------|------|
| 5/11 修法無效，FORCE_SEND 仍為空字串 | 改用 `inputs.force \|\| 'true'` fallback 寫法 |
| TWSE 外資欄位只有 sub-rows 無合計列 | 改為 sum 所有 "外資" key |
| TAIFEX HTML 多層 MultiIndex 解析失敗 | 改用 CSV DateDown endpoint |
| init 腳本中文輸出亂碼 | `sys.stdout.reconfigure(encoding="utf-8")` |
| fini_net 2024-2026 全為 null | init 時 TWSE rate limiting，後續 patch 補齊 |
